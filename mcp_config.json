{
  "mcpServers": {
    "kotlin-android": {
      "command": "python3",
      "args": [
        "simple_mcp_server.py"
      ],
      "cwd": "${MCP_SERVER_DIR}",
      "env": {
        "PROJECT_PATH": "${WORKSPACE_ROOT}",
        "WORKSPACE_PATH": "${WORKSPACE_ROOT}"
      }
    }
  }
}